import React from 'react';
import ReactDOM from 'react-dom';
import { MyThing } from './Comps';

ReactDOM.render(
    <React.StrictMode>
        <MyThing />
    </React.StrictMode>,
    document.getElementById('root')
);
